'use client'

import React from 'react'

interface HeatmapLayerProps {
  data: any[]
  intensity: number
}

export default function HeatmapLayer({ data, intensity }: HeatmapLayerProps) {
  // This component integrates with Leaflet.heat
  // For production, use leaflet-heat library
  return null
}
